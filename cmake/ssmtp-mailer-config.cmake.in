# ssmtp-mailer CMake configuration template
# This file is configured by CMake and installed with the package

@PACKAGE_INIT@

# Include the package configuration
include("${CMAKE_CURRENT_LIST_DIR}/ssmtp-mailer-targets.cmake")

# Set package variables
set(ssmtp-mailer_VERSION "@PROJECT_VERSION@")
set(ssmtp-mailer_VERSION_MAJOR "@PROJECT_VERSION_MAJOR@")
set(ssmtp-mailer_VERSION_MINOR "@PROJECT_VERSION_MINOR@")
set(ssmtp-mailer_VERSION_PATCH "@PROJECT_VERSION_PATCH@")

# Check if all required components are available
check_required_components(ssmtp-mailer)

# Set package properties
set_property(TARGET ssmtp-mailer-lib PROPERTY INTERFACE_INCLUDE_DIRECTORIES
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
    $<INSTALL_INTERFACE:include>
)

# Export package configuration
export(PACKAGE ssmtp-mailer)
